<style>
  .custom-banner {
    position: relative;
    min-height: 85vh;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    text-align: center;
    overflow: hidden;
    color: #fff;
  }

  .custom-banner::before {
    content: "";
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.45);
    backdrop-filter: blur(2px);
    z-index: 0; /* 👈 FIXED: put overlay behind content */
  }

  .custom-banner img.bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: -1; /* 👈 stays at very back */
  }

  /* === Strips === */
  .banner-top,
  .banner-content,
  .banner-footer {
    position: relative;
    z-index: 1; /* 👈 ensures content shows above overlay */
    padding: 1rem;
  }

  /* Top strip */
  .banner-top {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    font-size: 0.9rem;
    letter-spacing: 2px;
    text-transform: uppercase;
    background: rgba(0, 0, 0, 0.55);
    padding: 0.8rem 1.2rem;
    border-radius: 6px;
    display: inline-flex;
    margin: 1rem auto;
  }

  .banner-top img {
    height: 38px;
    width: auto;
  }

  /* Middle strip */
  .banner-content {
    max-width: 750px;
    margin: 0 auto;
    background: rgba(0, 0, 0, 0.45);
    padding: 3rem 2rem;
    border-radius: 8px;
  }

  .banner-content h1 {
    font-size: 3.2rem;
    font-weight: 800;
    margin-bottom: 1rem;
    line-height: 1.2;
  }

  .banner-content p {
    font-size: 1.2rem;
    margin-bottom: 2rem;
    line-height: 1.6;
    color: #f0f0f0;
  }

  .banner-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .banner-buttons a {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 1rem 2.2rem;
    font-weight: 600;
    text-decoration: none;
    font-size: 1.05rem;
    letter-spacing: 1px;
    border-radius: 6px;
    border: 2px solid #fff;
    transition: all 0.25s ease;
  }

  .btn-primary {
    background: #fff;
    color: #000;
  }

  .btn-primary:hover {
    background: transparent;
    color: #fff;
  }

  .btn-secondary {
    background: transparent;
    color: #fff;
  }

  .btn-secondary:hover {
    background: #fff;
    color: #000;
  }

  .banner-buttons a::after {
    content: "→";
    margin-left: 10px;
    font-size: 1.2rem;
    transition: transform 0.2s ease-in-out;
  }

  .banner-buttons a:hover::after {
    transform: translateX(6px);
  }

  /* Bottom strip */
  .banner-footer {
    font-size: 0.9rem;
    letter-spacing: 2px;
    text-transform: uppercase;
    background: rgba(0, 0, 0, 0.65);
    padding: 1rem;
    border-radius: 6px;
    display: inline-block;
    margin: 1rem auto;
  }

  /* Mobile tweaks */
  @media (max-width: 768px) {
    .custom-banner {
      min-height: 65vh;
    }
    .banner-top {
      flex-direction: column;
      gap: 0.5rem;
      font-size: 0.8rem;
    }
    .banner-content {
      padding: 2rem 1.2rem;
    }
    .banner-content h1 {
      font-size: 2.4rem;
    }
    .banner-content p {
      font-size: 1rem;
    }
    .banner-buttons a {
      padding: 0.8rem 1.5rem;
      font-size: 0.95rem;
    }
  }
</style>
